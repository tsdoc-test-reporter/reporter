import { Config } from '@jest/reporters';
import { globalConfigFactory } from '../../test-utils/factory/global-config';

export const reporterGlobalConfig: Config.GlobalConfig = globalConfigFactory();
